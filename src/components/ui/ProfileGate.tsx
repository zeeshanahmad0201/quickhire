import { Text } from 'react-native'
import { Screen } from '../base'

export const ProfileGate = () => {
    return (
        <Screen main centeredContent>
            <Text>Complete your profile to access this section.</Text>
        </Screen>
    )
}
